#include <stdio.h>

int main()
{
    /*
    char *s = "hello world";
    *s = 'H'; // "hello world" is constant so it can't change
    */

    int i = 1 // allocate memory 4 bytes
   

    int* ptr = NULL; // NULL is nothing and belong in kernel
    *ptr = 1;

    return 0;
}
